# API Projekt

## Erste Installationen: ##
# VS Code und Homebrew installiert, UV installiert, bei GitHub angemeldet und mit VS-Code verbunden
## Git Setup

# git config --global user.name "Yana" 
# git config --global user.email "ya.zi@gmx.de"
# uv init install, dann sind .pythonl-version; main.py; pyproject.toml gekommen
# uv init gemacht 
# Fast api installiert
# dann hab ich dieses app get und dann hab ich im browser hello world bekommen nachdem ich auf uvicorn gedrückt hab
# dann hab ich im url noch /docs ran und dann kam ne info zu meiner datei
